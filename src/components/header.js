import React from "react";
import { BoxHeader, HeaderTitle } from "../styled-components";

const Header = () => {
  return (
    <BoxHeader>
      <HeaderTitle>Personal budget</HeaderTitle>
    </BoxHeader>
  );
};
export default Header;
